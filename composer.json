{
    "name": "evinkuraga/tmdb-api",
    "type": "library",
    "description": "This is a fork of the package php-tmdb/api. Instead of waiting for the maintainer to update the php-tmdb/api package to reflect the latest changes from the API, I've decided to fix these. These packages are actively used for http://flixi.com, which means I will most likely maintain these packages with the latest changes that TMDB implements.",
    "homepage": "https://github.com/evinkuraga/api",
    "keywords": ["tmdb", "api", "php", "wrapper", "movie", "tv", "tv show", "tvdb"],
    "license": "MIT",
    "authors": [
        {
            "name": "OA: Michael Roterman",
            "homepage": "http://wtfz.net",
            "email": "michael@wtfz.net"
        },
        {
            "name": "Maintainer: Evin Kuraga"
        }
    ],
    "require": {
        "php": ">=5.6.0",
        "ext-curl": "*",
        "symfony/event-dispatcher": ">=2.4,<8",
        "symfony/options-resolver": ">=2.6,<8",
        "guzzlehttp/guzzle": ">=6.0,<8",
        "psr/log": "~1.0",
        "doctrine/cache": "^1.6",
        "kevinrob/guzzle-cache-middleware": "^2.0 || ^3.0",
        "rtheunissen/guzzle-log-middleware": "^0.4.0"
    },
    "require-dev": {
        "phpunit/phpunit": ">=5.7,<7",
        "monolog/monolog": ">=1.11.0"
    },
    "suggest": {
        "monolog/monolog": "Required if you want to make use of logging features."
    },
    "autoload": {
        "psr-4": { "Tmdb\\": "lib/Tmdb" }
    }
}
